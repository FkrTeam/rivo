{
  "name": "rivo",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "RIVO - architectural millwork. Corporate website.",
  "scripts": {
    "pages": "node scripts/build-pages.mjs",
    "predev": "npm run pages",
    "dev": "vite",
    "prebuild": "npm run pages",
    "build": "vite build",
    "preview": "vite preview",
    "assets:panel": "node scripts/build-panel-glb.mjs",
    "assets:images": "node scripts/optimize-images.mjs",
    "assets": "npm run assets:panel && npm run assets:images",
    "check:framing": "node scripts/frame-check.mjs"
  },
  "dependencies": {
    "gsap": "^3.15.0",
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@fontsource-variable/inter": "^5.3.0",
    "sharp": "^0.35.4",
    "vite": "^8.2.2"
  }
}
